{{> header.hbs}}
    <h1>Médicos</h1>
    <div class="btn-group">
        <a class="btn btn-primary" href="/medics">Criar um novo Médico</a>
        <a class="btn btn-warning" href="/">Voltar</a>
    </div>
    <br>
    <br>
    <table class="table table-hover">
        <tr>
            <th>ID</th>
            <th>Nome</th>
        </tr>
        {{#each data}}
        <tr>
            <td>{{id}}</td>
            <td><a href="/medics/{{id}}">{{name}}</a></td>
            <td>{{email}}</td>
            <!--<td><a class="btn btn-danger btn-sm" href="/customers/{{id}}/delete"><i class="glyphicon glyphicon-trash"></i></a></td>-->
        </tr>
        {{/each}}
    </table>

    {{#if delete}}
        <div class="success">Cliente deletado com sucesso</div>
    {{/if}}
{{> footer.hbs}}